import random


def test_error_generator() -> None:
    random.seed(42)

    # error_lexicon = ErrorLexicon(
    #     [
    #         Entry("apple", 1),
    #         Entry("look", 2),
    #         Entry("cherry", 3),
    #     ]
    # )
    # error_generator = ErrorGenerator(error_lexicon=error_lexicon)
    # sentence = "I like apple."
    # assert error_generator(sentence) == "I like apple."
